#pragma once

namespace ui {
    enum class Alignment {
        Left,
        Center,
        Right,
    };

    enum class VerticalAlignment {
        Top,
        Middle,
        Bottom,
    };
} // namespace ui
